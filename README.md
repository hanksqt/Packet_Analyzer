# netsniff — a packet analyzer built from raw bytes

Captures Ethernet frames (live, or offline from a `.pcap`), decodes every protocol
layer by hand, tracks conversations, and reports protocol breakdowns, top talkers
and simple anomalies. No scapy — the decoders parse the wire themselves.

> Phase 0 scaffold. The full README lands in Phase 10.
