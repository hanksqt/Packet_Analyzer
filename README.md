# Packet_Analyzer
A Python tool that captures network packets, live or from a .pcap file, and decodes each protocol layer by hand: Ethernet, IPv4/IPv6, TCP, UDP, ICMP, and ARP. It tracks conversations and shows traffic stats like top talkers and protocol breakdowns. The decoding is written from raw bytes, not with scapy
